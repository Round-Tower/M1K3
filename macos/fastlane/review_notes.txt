Thank you for reviewing M1K3.

NO ACCOUNT REQUIRED — just launch and use it.

FIRST LAUNCH:
  Mac: on most Macs, uses Apple Intelligence (instant, no download).
  iPhone/iPad: downloads a local AI model (~2 GB). After setup, fully offline.
  Without Apple Intelligence on any platform, downloads a local model (~630 MB–2 GB).

OUTBOUND (network.client): model download + optional web search (OFF by default). No analytics, tracking, or telemetry.

INBOUND (network.server): two features, both OFF by default in Settings > Privacy:
  1. MCP server — loopback only (127.0.0.1:4242), lets AI coding agents call M1K3's tools. Verify: /Applications/M1K3.app/Contents/Helpers/m1k3 status
  2. Brain at Home — LAN service for QR-paired iPhone/iPad only. TLS-PSK, private interfaces. Verify: dns-sd -B _m1k3._tcp
The helper (Contents/Helpers/m1k3) is a client only; no server entitlement.

CONTENT CONTROLS (DeclaredAgeRange): uses Apple's Declared Age Range API (gates 13/16/18). Under 16: web tools removed from the agent palette (code gate). All minors: age-appropriate tone. Declining = full capability. Only a coarse band enum is persisted.
  Mac: Settings > Privacy tab > Content Controls > "Set up"
  iPhone/iPad: Settings > Content Controls > "Set up"

CALENDAR (calendars entitlement): optional context sense, OFF by default. Settings > Privacy > Calendar toggle. Peeks upcoming events via EventKit for time-aware responses. On-device only, TCC-gated.

LOCATION (location entitlement): optional context sense, OFF by default. Settings > Privacy > Location toggle. One-shot reading at km accuracy. On-device only, TCC-gated.

SCREEN RECORDING (ScreenCaptureKit): system AUDIO capture only, for call transcription (far-end speaker). SCStream: capturesAudio=true, video 2x2@1fps (minimum; no frames consumed). Degrades to mono mic if denied. No screen-capture entitlement held. All audio processed on-device via WhisperKit.

MICROPHONE: on-device speech-to-text. Never transmitted.
FILES: user-selected notes/PDFs only. Processed locally.

HOW TO TEST:
  1. Finish onboarding + model download
  2. Chat, then voice mode
  3. Drag in a note/PDF and ask about it
  4. Memories > "forget" an item
  5. Content Controls:
     Mac: Settings > Privacy tab > Content Controls > "Set up"
     iPhone/iPad: Settings > Content Controls > "Set up"

Happy to clarify anything — thank you.
