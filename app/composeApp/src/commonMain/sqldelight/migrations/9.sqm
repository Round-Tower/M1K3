-- Migration 8 → 9: retire the pixel pet (tamagotchi) subsystem.
--
-- PixelPetState/PetViewModel/PetMetricsRepository and the pet UI (stat
-- bars, interaction overlay, pixel-art renderer, demo screen) are cut in
-- the same reduction pass that removed EcoMetrics (migration 8) — see
-- .claude/project-memory.md Wave 2. PetMetrics stored happiness/energy/
-- health snapshots + lifetime eco totals for a feature with zero
-- production call sites (PixelPetView/PixelPetViewCompact were declared
-- but never wired into any real screen).
--
-- SQLite: nothing references PetMetrics as a FK target. Safe, one-way
-- drop, same shape as migrations 7 and 8.
--
-- MurphySig: kev+claude / confidence 0.9 / 2026-08-20

DROP TABLE IF EXISTS PetMetrics;
